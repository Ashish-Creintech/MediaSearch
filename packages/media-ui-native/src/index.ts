// media-ui-native: same contract as media-ui-react (Grid, Lightbox,
// ReelSwiper) rebuilt with React Native primitives (FlatList, Modal, etc.)
// instead of DOM. Same rule: no imports from media-core or media-native.
//
// If time is short, this is the safest package to leave as a stub —
// document that choice in the README rather than half-finishing it.

export {};
